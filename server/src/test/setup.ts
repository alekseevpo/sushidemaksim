// Vitest setup file
import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
    // console.log('Backend Test Suite Starting...');
});

afterAll(() => {
    // console.log('Backend Test Suite Finished.');
});
