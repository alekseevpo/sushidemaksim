# Changelog

Все значимые изменения в этом проекте документируются в этом файле на основе системного журнала разработок.

## [2026-04-19] — Выбор напитков и управление доступностью магазина

### 🎯 Цели сессии

- Реализация выбора вкуса для напитков в корзине.
- Улучшение видимости выбора в имейлах и админ-панели.
- Внедрение системы «Закрыть на сегодня» для оперативного управления заказами.

---

### 🥤 1. Выбор вкуса напитков (Beverage selection)

**Реализовано:**

- **UI в корзине**: В `CartItemList.tsx` добавлен выпадающий список для напитков (ID 116), позволяющий выбрать Coca-Cola, Fanta или Sprite.
- **Сохранение в БД**: Обновлены схемы и API для передачи поля `selected_option`.
- **Видимость**:
    - В **админ-панели** (`AdminOrders.tsx`) выбранный вкус теперь отображается оранжевым бейджем прямо под названием товара.
    - В **email-уведомлениях** добавлена строка с выбранной опцией.
    - В **WhatsApp-заметках** вкус дублируется в квадратных скобках.

### 🏪 2. Управление доступностью магазина

**Проблема:** Менеджеру требовался способ быстро закрыть прием заказов на текущий день (например, при большой нагрузке), оставляя возможность предзаказа на завтра.

**Реализовано:**

- **Admin Toggle**: В `AdminSettings.tsx` добавлен тумблер `isTodayClosed`.
- **Интеллектуальный баннер**: `StoreStatusBanner.tsx` теперь показывает статус «Cerrado para hoy» с подсказкой о возможности предзаказа.
- **Логика корзины**: При активном режиме «Закрыть на сегодня» дата заказа автоматически переключается на завтра, а текущий день блокируется в календаре.

### 🛡️ 3. Стабильность и Качество

**Исправлено:**

- **Linting**: Исправлены ошибки форматирования и опечатки в `StoreStatusBanner.tsx` и `AdminOrders.tsx`.
- **Тестирование**: Пройдены все **96 unit-тестов** и выполнена выборочная проверка **E2E Playwright**.

---

## [2026-04-18] — Стабилизация сайта и предотвращение циклов перезагрузки

### 🎯 Цели сессии

- Устранение бесконечных циклов автоматического обновления страницы при смене домена.
- Повышение отказоустойчивости приложения при сетевых сбоях и ошибках загрузки ресурсов (ChunkLoadError).
- Настройка более предсказуемого поведения PWA на период миграции DNS.

---

### 🛡️ 1. Защита от бесконечных перезагрузок (SafeReload)

**Проблема:** При привязке нового домена DNS-записи могут «мигать», вызывая временные ошибки 404 для файлов сайта. Старый код реагировал на это мгновенной перезагрузкой, что приводило к бесконечному циклу «рефреша» для пользователя.

**Реализовано:**

- **Утилита `safeReload`**: Создан централизованный механизм в `src/utils/reload.ts`, который ограничивает количество автоматических перезагрузок (макс. 3 в минуту). При превышении лимита рефреш блокируется, а ошибка выводится в консоль.
- **Интеграция**: Утилита внедрена в `ErrorBoundary.tsx` и механизм `lazyRetry` в `App.tsx`.
- **Логирование**: Каждая защищенная перезагрузка теперь сопровождается подробным логом в консоли с указанием причины (`Reason: ...`).

### 📱 2. Оптимизация PWA и инфраструктуры

**Реализовано:**

- **VitePWA Config**: Режим регистрации изменен с `autoUpdate` на `prompt`. Это предотвращает принудительные перезагрузки вкладки сервис-воркером в моменты, когда Vercel обновляет кэш или конфигурацию домена.
- **Search Console**: Добавлен мета-тег для верификации владения доменом в Google Search Console.
- **Formatting Fixes**: Исправлены ошибки форматирования (Prettier/ESLint) в `ContactsPage.tsx` и `reload.ts`, выявленные линтером.

---

## [2026-04-16] — Фикс инвайтов, унификация ввода телефона и лимиты адресов

### 🎯 Цели сессии

- Исправление и оптимизация функционала «Que me inviten» (приглашение к оплате).
- Унификация полей ввода номера телефона по всему проекту.
- Локализация интерфейса личного кабинета (устранение английских строк).
- Внедрение лимита на количество сохраняемых адресов (max 5).

---

### 📩 1. Исправление ссылок приглашений (Social Sharing)

**Проблема:** Ссылки для Telegram/WhatsApp генерировались некорректно (localhost), отсутствовали превью, и UI «зависал» на спиннере при отправке.

**Реализовано:**

- **Абсолютные URL**: Все ссылки теперь генерируются на основе `config.frontendUrl`, что гарантирует их работу как в локалке, так и в продакшене.
- **Оптимизация превью**: Сгенерирован и внедрен премиальный визуал `hungry-panda.webp` (WebP, 600x600, 25KB). Обновлены мета-теги OpenGraph для корректного отображения в мессенджерах.
- **UX Fix**: Устранено «зависание» кнопки инвайта в корзине; состояние загрузки теперь сбрасывается сразу после открытия окна шаринга.
- **Чистка роутов**: Удалены дублирующиеся обработчики в `index.ts`, логика редиректа унифицирована в `orders.ts`.

### 📞 2. Унификация ввода номера телефона

**Проблема:** В профиле и адресах телефон вводился как обычный текст, в отличие от корзины, где есть маска и префикс.

**Реализовано:**

- **AddressesTab & ProfileTab**: Внедрен единый компонент ввода с фиксированным префиксом `+34`.
- **Валидация**: Реализована строгая фильтрация (только цифры) и ограничение до 9 символов.
- **Стилизация**: Дизайн полей приведен к «золотому стандарту» checkout (высота 46px, серый блок префикса, премиальные скругления).

### 📍 3. Лимит адресов и локализация

**Реализовано:**

- **Лимит 5 адресов**:
    - **Backend**: В `POST /api/user/addresses` добавлена проверка. При попытке добавить 6-й адрес сервер возвращает ошибку 400.
    - **Frontend**: Внедрен счетчик `(x/5)` и автоматическая замена кнопки «AÑADIR DIRECCIÓN» на инфо-блок с предупреждением при достижении лимита.
- **Fix Localization**: Бейдж основного адреса изменен с «DEFAULT» на испанский «PRINCIPAL».

---

## [2026-04-14] — Оптимизация загрузки, стабилизация тестов и редизайн UI мобильного меню

### 🎯 Цели сессии

- Оптимизация процесса загрузки и устранение визуальных "скачков" (Layout Shifts).
- Стабилизация всех E2E-тестов (Playwright).
- Визуальный редизайн мобильного меню навигации.

---

### 🎨 1. Улучшение UI и Редизайн Мобильного Меню

**Реализовано:**

- **Минималистичная навигация**: Ссылки мобильного меню теперь отображаются заглавными буквами и строго по центру. Фоновое выделение активного пункта заменено на стильное нижнее подчеркивание.
- **Позиционирование иконок**: Полностью удалены иконки из ссылок для создания более чистого текстового интерфейса (по запросу пользователя). Горизонтальное меню категорий подтянуто на 1px вверх через `calc()` для перекрытия микро-щелей на Retina-экранах.

### 🚀 2. Устранение Layout Shifts (CLS)

**Проблема:** При загрузке данных скелетоны имели разную высоту с финальным контентом, что вызывало рывок страницы при переключении.

**Реализовано:**

- **Точные Skeleton-компоненты**: `HomeSkeleton` и `MenuSkeleton` хирургически подогнаны под размеры реальной верстки (особенно Hero-блок в `100svh`).
- **Early-return Архитектура**: В `HomePage` и `MenuPage` реализованы строгие возвраты скелетона на этапе `isLoading`, что исключило мерцания и частичный рендеринг компонентов (например, `<StoreStatusBanner>`) до получения данных.

### 🛡️ 3. Стабилизация E2E-тестов (Playwright)

**Проблема:** Структурные изменения и скелетоны нарушили работу тестов.

**Исправлено:**

- **Sync to Async Locator**: В Vitest-тестах синхронные `getBy` заменены на асинхронные `findBy` для ожидания разрешения скелетонов.
- **API Mocking**: В Playwright (`checkout-guest-address.spec.ts`) добавлен мок для `/api/categories`, чтобы скелетон страницы меню успешно "разрешался" в тестах.
- **Strict Mode & Text Matchers**: Устранен конфликт с двумя тегами `<header>` за счет использования `getByRole('banner')`. Уточнены регулярные выражения для проверки Toast-сообщений регистрации.
- **Итог**: Все **24 E2E-теста** и **96 unit-тестов** успешно проходят.

### 🐛 4. Исправление багов и стабилизация CI

**Реализовано:**

- **ESLint Clean-up**: Удалены неиспользуемые переменные (`ChevronRight`, `Icon`) в `Header.tsx`, блокировавшие билд в GitHub Actions.
- **Backend Type Safety**: Исправлена ошибка типов в `reservations.ts`, где объект `Date` ошибочно передавался вместо строки в валидатор рабочих часов.
- **Исправление выбора адреса**: В `CartPage` исправлен маппинг данных при выборе сохраненного адреса из профиля. Добавлена функция автоматического определения зоны доставки (`detectZone`) на основе сохраненных координат, что решило проблему неактивных кнопок при выборе дома/работы.
- **Prettier formatting**: Весь измененный код приведен к единому стилю для прохождения проверок линтера.

---

## [2026-04-13] — Системные улучшения уведомлений, безопасности и UI

### 🎯 Цели сессии

- Улучшение системы уведомлений администратора (звук и новые события).
- Финализация логики управления пользователями (Архивация/Удаление).
- Доработка визуальных деталей интерфейса (Профиль, Модальные окна).
- Обеспечение стабильности через тестирование.

---

### 🔊 1. Улучшение системы уведомлений (Admin Panel)

**Проблема:** Администратор не слышал звуковых сигналов при поступлении новых заказов из-за политики браузера по блокировке автоплея (Audio Autoplay Policy).

**Реализовано:**

- **playAlert Helper**: Создан централизованный механизм воспроизведения звука в `AdminPage.tsx` с обработкой ошибок и логированием блокировок браузером.
- **Расширение событий**: Звуковые уведомления теперь срабатывают не только на **Заказы (Pedidos)**, но и на новые **Бронирования столов (Reservas)**.
- **Интерактивная активация**: При включении звука в интерфейсе администратора теперь проигрывается "тестовый сигнал". Это позволяет пользователю проверить громкость и одновременно "разблокирует" аудио-контекст браузера для работы в фоновых вкладках.
- **Оптимизация опроса**: Интервал проверки бронирований сокращен с 60 до 30 секунд для синхронизации с заказами.

### 👤 2. Управление пользователями и Безопасность

**Реализовано:**

- **Двухэтапное удаление**:
    - Первое удаление переводит пользователя в статус **«Архив»** (soft delete).
    - Второе удаление из архива полностью очищает данные (hard delete), включая историю заказов, адреса и избранное.
- **Восстановление**: Добавлена возможность вернуть пользователя из архива в активное состояние.
- **Автоматическая чистка при регистрации**: Если новый пользователь регистрируется с email, который находится в архиве, система автоматически удаляет архивную запись, обеспечивая чистую регистрацию.
- **UI индикация**: Архивные пользователи в панели управления теперь визуально выделяются (grayscale/opacity), чтобы администратору было проще их идентифицировать.

### 🍱 3. UI/UX и Визуальные улучшения

**Реализовано:**

- **Quick Access в Header**: В выпадающее меню пользователя на десктопе добавлены прямые ссылки на «Мои заказы», «Мои адреса» и «Избранное».
- **AddressModal**: Исправлено выравнивание ценников доставки. Теперь все карточки имеют одинаковую структуру и горизонтальное выравнивание, независимо от длины названия зоны.
- **Birthday Format**: В личном кабинете формат даты рождения изменен с `ДД/ММ/ГГГГ` на `ДД.ММ.ГГГГ` по просьбе пользователя.
- **Header Optimization**: Уменьшены отступы в меню и обеспечено полное отображение email без обрезки текста.

### 🛠️ 4. Стабильность и Инфраструктура

**Исправлено:**

- **Vitest Regression**: Устранена ошибка в `storeStatus.ts`, из-за которой тесты падали при обработке дат (ошибка `split is not a function`).
- **Тестирование**: Все **96 unit-тестов** проходят успешно. Проведена верификация основных сценариев через **Playwright (E2E)**.
- **Git**: Проведен финальный коммит и пуш вех изменений в ветку `main`.
